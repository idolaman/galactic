import { AppDialogNotice } from "@/components/app/AppDialogNotice";

export const WorkspaceIsolationDialogServicesNotice = () => (
  <AppDialogNotice>
    Project Services creates stable local routes for each active workspace. Start
    services on the listed ports and Galactic handles the workspace-specific
    hostnames.
  </AppDialogNotice>
);
